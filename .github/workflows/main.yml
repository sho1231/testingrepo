name: Testing
on: [push]
jobs:
  testworkflow:
    runs-on: windows-latest
    strategy:
      matrix:
        version: [8,10,11]
    steps:
    - run: echo "hello"
      with:
        version: ${{ matrix.version }}

    
