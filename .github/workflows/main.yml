name: Testing
on: [push]
jobs:
  testworkflow:
    strategy:
      matrix:
        os: [windows-latest]
        version: [8,10,11]
    steps:
    - run: echo "hello"
      with:
        version: ${{ matrix.version }}

    
