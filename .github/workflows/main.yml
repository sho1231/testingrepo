name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: push
jobs:
  testworkflow:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
      - run: echo "world"

    
